# Session Memo

## P1: Session Context

session_id: ses_13eabac84ffeUqiEGzp64XidJC
cwd: /Users/ibkim/Projects/voice-to-textnote
event: PreCompact

## 2026-06-14: SPEC-MOBILE-002 / SPEC-REFACTOR-001 / SPEC-TONE-001 Handoff

### User Objective

`SPEC-MOBILE-002`, `SPEC-REFACTOR-001`, `SPEC-TONE-001`를 프로덕션용으로 100%가 될 때까지 진행.

### Current Goal State

- Historical state: the goal was previously marked `blocked` after repeated native mobile verification blockers.
- Superseded by the later `2026-06-14: Mobile Native Verification Resolved` section below.
- REFACTOR/TONE were verified green; MOBILE native build/test evidence is now also available.

### Backend Evidence

- `cd backend && ../venv/bin/python -m ruff check .` -> `All checks passed!`
- `cd backend && ../venv/bin/python -m mypy .` -> `Success: no issues found in 394 source files`
- Focused route/tone suite:
  `../venv/bin/python -m pytest -q tests/unit/test_route_registry_invariance.py tests/unit/test_tone_task.py tests/unit/test_tone_api.py tests/unit/test_tone_engine.py -o addopts=""`
  -> `52 passed, 6 warnings`
- Full backend suite:
  `venv/bin/python -m pytest backend -q`
  -> `3323 passed, 16 skipped, 255 warnings`, total coverage `98.62%`
- REFACTOR invariant: `find backend/app/api/v1 -maxdepth 1 -type f -name '*.py' ! -name '__init__.py' ! -name 'registry.py' -print` -> no output.

### Mobile Work Completed

- `client/lib/services/local_stt_service.dart`: service core split behind `LocalSttModelSource` and `LocalSttRuntime`.
- `client/lib/services/local_stt_provider.dart`: Riverpod provider wiring `SttModelManager` to `WhisperGgmlLocalSttRuntime`.
- `client/lib/services/local_stt_runtime_whisper.dart`: `whisper_ggml_plus` FFI runtime implementation.
- `client/pubspec.yaml` / `client/pubspec.lock`: `whisper_ggml_plus 1.5.2` and transitive `freezed_annotation 3.1.0`.
- Recording changed to `.wav`, `AudioEncoder.wav`, 16kHz, mono for whisper input compatibility.
- Removed local STT MethodChannel stub path; Android/iOS manual STT stubs are not used.
- `client/analysis_options.yaml`: excludes `build/**` and `lib/dataconnect_generated/**`.
- `.github/workflows/mobile.yml`: Flutter mobile CI for `pub get`, `analyze`, `test`, Android debug APK, iOS debug no-codesign.
- `client/scripts/verify_mobile.sh`: local equivalent mobile verification script.
- `client/android/app/build.gradle`: explicit Android/Kotlin/Flutter/Google Services plugins.
- `client/ios/Podfile`: `platform :ios, '15.0'` and `IPHONEOS_DEPLOYMENT_TARGET=15.0`.

### Mobile Evidence

- `flutter pub get --offline` with copied SDK -> `Got dependencies!`
- `dart run tool/local_stt_smoke.dart` -> `local_stt_smoke: PASS`
- Full `flutter analyze` -> `No issues found!`
- `.github/workflows/mobile.yml` and `client/analysis_options.yaml` parse with `yaml.safe_load`.
- `bash -n client/scripts/verify_mobile.sh` -> pass.
- `ruby -c client/ios/Podfile` -> `Syntax OK`.
- `git diff --check` over mobile/doc touched paths -> pass.

### Historical Blockers

- These blockers are retained as historical context only; see the resolved section below for current status.
- `flutter test test/services/local_stt_service_test.dart` previously failed before test code loaded because Flutter tester could not create `127.0.0.1:0` server socket in that sandbox:
  `Failed to create server socket (OS Error: Operation not permitted, errno = 1)`.
- `flutter build apk --debug` previously failed before Gradle/code stage:
  `No Android SDK found. Try setting the ANDROID_HOME environment variable.`
  The SDK is now installed at `/Users/ibkim/Library/Android/sdk`.
- `cd client/ios && HOME=/private/tmp pod install` previously failed because network access to CocoaPods trunk was blocked:
  `Unable to add a source with url https://cdn.cocoapods.org/ named trunk`.
- `flutter build ios --debug --no-codesign` previously failed on Xcode/CoreSimulator/SwiftPM cache permissions:
  `CoreSimulatorService connection became invalid`, `Operation not permitted`.

### Important Hygiene Notes

- Running copied Flutter SDK from `/private/tmp/flutter-codex.fn8k7a` rewrites `client/android/local.properties` to that path. Always restore it to:
  `flutter.sdk=/opt/homebrew/share/flutter`
  before finalizing.
- `.dart_tool/**` and `build/**` may contain copied-SDK paths and failed build artifacts; they are ignored and should not be used as completion evidence.
- `client/build/ios/SourcePackages` was generated by failed iOS build and may remain ignored.

### Historical Completion Path

This path has now been executed locally.

1. Run mobile CI on a GitHub runner, or locally install/provide Android SDK + Gradle + CocoaPods trunk access + usable Xcode caches.
2. Execute `client/scripts/verify_mobile.sh` or equivalent CI jobs.
3. Require proof lines for:
   - `flutter analyze` -> `No issues found!`
   - `flutter test` -> all tests pass
   - `flutter build apk --debug` -> APK produced
   - macOS/iOS environment: `flutter build ios --debug --no-codesign` -> build succeeds
4. Only then consider SPEC-MOBILE-002 native production readiness fully proven.

## 2026-06-14: Mobile Native Verification Resolved

### Outcome

- CocoaPods CDN access was rechecked and is currently reachable:
  `curl -I https://cdn.cocoapods.org/` -> `HTTP/2 200`.
- `cd client/ios && pod install` now succeeds:
  `Pod installation complete! There are 5 dependencies from the Podfile and 5 total pods installed.`
- iOS no-codesign build now succeeds:
  `flutter build ios --debug --no-codesign` -> `✓ Built build/ios/iphoneos/Runner.app`.
- Android SDK was installed at `/Users/ibkim/Library/Android/sdk` and persisted with:
  `flutter config --android-sdk /Users/ibkim/Library/Android/sdk`.
- `flutter doctor -v` now recognizes Android:
  `Android SDK version 36.0.0`, `All Android licenses accepted`.
- Android APK build now succeeds:
  `flutter build apk --debug` -> `✓ Built build/app/outputs/flutter-apk/app-debug.apk`.
- Full native mobile verification now succeeds:
  `cd client && ./scripts/verify_mobile.sh --native` -> analyzer clean, all tests pass, local STT smoke pass, Android APK built, iOS Runner.app built.
- Current Flutter verification:
  `cd client && flutter analyze` -> `No issues found!`
  `cd client && flutter test` -> `324 passed`
- Android network security static regression was added:
  `client/test/config/network_security_config_test.dart` -> AndroidManifest networkSecurityConfig reference, default cleartext denial, and localhost/Tailscale-only cleartext exceptions.
- Android network security was split by variant:
  `client/android/app/src/main/res/xml/network_security_config.xml` is now the Release/Profile baseline with no cleartext domain exceptions.
  `client/android/app/src/debug/res/xml/network_security_config.xml` contains the only localhost/Tailscale cleartext exceptions.
- Android release/profile split verification:
  `cd client && flutter test test/config/network_security_config_test.dart` -> `4 passed`;
  `cd client && flutter build apk --release` -> `✓ Built build/app/outputs/flutter-apk/app-release.apk`;
  `cd client && flutter build apk --debug` -> `✓ Built build/app/outputs/flutter-apk/app-debug.apk`.
- Release readiness preflight was added:
  `client/scripts/verify_release_readiness.py` -> default mode validates repo-local Firebase config, iOS APNs entitlement, App Store metadata, backend Push wiring, Android network policy, and E2E docs.
  `--strict` requires `FIREBASE_CREDENTIALS_PATH`, APNs key metadata, App Store Connect API key metadata, Android/iOS physical device IDs, and a Firebase test device token.
  It also verifies release docs have no unresolved placeholders, `ANDROID_DEVICE_SERIAL` via `adb devices -l`, and `IOS_DEVICE_UDID` via `xcrun devicectl list devices`, so physical-device E2E cannot be marked ready by environment variables alone.
- Privacy policy release placeholders were removed. The policy now uses `privacy@voicetextnote.com` for privacy inquiries and points legal mailing address disclosure to App Store Connect seller/store metadata instead of an unknown placeholder address.

### Durable Fixes

- Restored Android Gradle wrapper under `client/android/gradlew*` and `client/android/gradle/wrapper/*`.
- Added Flutter Gradle composite build wiring in `client/android/settings.gradle`.
- Aligned Android tooling with Flutter 3.44 expectations: Gradle 8.14, AGP 8.11.1, Kotlin Gradle Plugin 2.2.20, compileSdk 36.
- Enabled Android core library desugaring for `flutter_local_notifications`.
- Set Android Java source/target compatibility to 17 to match Kotlin debug target.
- Fixed Android foreground notification icon from missing `R.drawable.ic_launcher_foreground` to existing `R.mipmap.ic_launcher`.
- Added `client/ios/Flutter/Profile.xcconfig` and wired Runner Profile configuration to include Pods profile settings.
- Fixed iOS `AppDelegate.swift` MethodChannel handling to use Swift `FlutterResult` callback style (`result(true)`) and avoid compiler type-resolution issues.

### Remaining Notes

- `client/android/local.properties` is local environment state. Keep machine-specific SDK paths out of committed changes unless intentionally documenting a local-only setup.
- `.omx/metrics.json` and `.omx/state/tmux-hook-state.json` are runtime state and should not be included in feature commits.
- Flutter currently emits Built-in Kotlin migration warnings for the app/plugins. They are not failing the build today, but future Flutter versions may require a separate migration.
